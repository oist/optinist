import os
from optinist.api.dir_path import DIRPATH
configfile: os.path.join(DIRPATH.ROOT_DIR, 'config.yaml')

for rule, details in config["rules"].items():
    include: details.get("rule_file")

rule all:
    input: config["last_output"]